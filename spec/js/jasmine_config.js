// Stuff to load for every test
import $ from 'jquery';
import 'jasmine-jquery/lib/jasmine-jquery';
import "babel-polyfill";
// We're also including 'jasmine-fixtures' directly from the Karma config file

// Tell jasmine where to load the fixtures (commented for now)
// jasmine.getFixtures().fixturesPath = 'base/spec/js/fixtures';
